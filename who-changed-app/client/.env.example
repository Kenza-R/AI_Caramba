# Optional: full API origin when not using the Vite dev proxy (e.g. vite preview or mobile on LAN)
# VITE_API_URL=http://127.0.0.1:3001

# Optional: port the Vite proxy forwards /api to (default 3001)
# VITE_API_PORT=3001
